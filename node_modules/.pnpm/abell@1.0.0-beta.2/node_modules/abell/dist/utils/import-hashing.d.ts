/**
 * But hey what is import hashing?
 *
 * Checkout PR description of https://github.com/abelljs/abell/pull/161#issue-1906511077 for more info
 *
 */
export declare const CSS_FETCH_REGEX: RegExp;
export declare const JS_FETCH_REGEX: RegExp;
export declare const getCSSLinks: (templatePage: string) => string;
export declare const getJSLinks: (templatePage: string) => string;
/**
 * Returns the previously transformed page.
 *
 * We can copy paste imports from this to our new pages
 */
export declare const getImportTemplatePage: ({ ROOT, OUTPUT_DIR, importTemplatesMemo, importsHash }: {
    ROOT: string;
    OUTPUT_DIR: string;
    importTemplatesMemo: Record<string, {
        htmlPath: string;
    }>;
    importsHash: string;
}) => string;
export declare const getImportsHash: ({ ROOT, appHtml, htmlPath }: {
    ROOT: string;
    appHtml: string;
    htmlPath: string;
}) => {
    importsStringDump: string;
    importsHash: string;
};
